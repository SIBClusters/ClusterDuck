<h2>This is the index page for members of <%= @group.name %></h2>

<h3>Member list</h3>
  <ul>
    <%= @members.each do |member| %>
    <li><%= member.name %></li>
    <% end %>
  </ul>


<div>
  <%= link_to 'Add Member', new_group_member_path(@group), :class => 'btn' %>
</div>


